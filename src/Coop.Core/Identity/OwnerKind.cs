namespace Coop.Core.Identity
{
    /// <summary>Which kind of object owns a <c>PartyBase</c> — see <see cref="PartyBaseId"/>.</summary>
    public enum OwnerKind
    {
        MobileParty,
        Settlement,
    }
}
